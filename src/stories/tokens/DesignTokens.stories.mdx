import { Meta, Story, Preview } from '@storybook/addon-docs';
import { space } from '../../tokens/space';
import { font } from '../../tokens/font';
import { color } from '../../tokens/color';
import { typography } from '../../tokens/typography';

<Meta title="Design System/Design Tokens" />

# Design Tokens

## Spacing

<table>
  <thead><tr><th>Name</th><th>Value</th><th>Preview</th></tr></thead>
  <tbody>
    {Object.entries(space).map(([key, value]) => (
      <tr key={key}>
        <td>{key}</td>
        <td>{value}</td>
        <td>
          <div style={{ width: value, height: '16px', background: '#eee', display: 'inline-block' }} />
        </td>
      </tr>
    ))}
  </tbody>
</table>

## Colors

<table>
  <thead><tr><th>Name</th><th>Value</th><th>Preview</th></tr></thead>
  <tbody>
    {Object.entries(color).map(([key, value]) => (
      <tr key={key}>
        <td>{key}</td>
        <td>{value}</td>
        <td>
          <div style={{ width: 32, height: 16, background: value, border: '1px solid #ccc', display: 'inline-block' }} />
        </td>
      </tr>
    ))}
  </tbody>
</table>

## Font

<table>
  <thead><tr><th>Type</th><th>Value</th></tr></thead>
  <tbody>
    <tr><td>Family (sans)</td><td>{font.family.sans}</td></tr>
    <tr><td>Family (mono)</td><td>{font.family.mono}</td></tr>
    <tr><td>Size (md)</td><td>{font.size.md}</td></tr>
    <tr><td>Weight (bold)</td><td>{font.weight.bold}</td></tr>
  </tbody>
</table>

## Typography

<table>
  <thead><tr><th>Type</th><th>Preview</th><th>Token</th></tr></thead>
  <tbody>
    {Object.entries(typography).map(([key, value]) => (
      <tr key={key}>
        <td>{key}</td>
        <td>
          <span style={{ fontSize: value.fontSize, fontWeight: value.fontWeight, lineHeight: value.lineHeight }}>
            {key.charAt(0).toUpperCase() + key.slice(1)} Example
          </span>
        </td>
        <td>
          {JSON.stringify(value)}
        </td>
      </tr>
    ))}
  </tbody>
</table>
